[advisories]
ignore = [
    # rsa 0.9.10 via optional sqlx-mysql; mysql feature is disabled and not used in this workspace.
    "RUSTSEC-2023-0071",
    # glib unsound via webkit2gtk/Tauri; upstream issue, no fix available
    "RUSTSEC-2024-0429",
]
